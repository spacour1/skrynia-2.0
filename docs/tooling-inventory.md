# Tooling inventory

Review date: **2026-08-27**
Stage 0 input SHA: `7d1ef6fc6b34bd11e8af106803acbcb819a94262`
Stage 1 baseline SHA: `effe378949bd0f246abb96b60dad2e83ff86ed38`

This inventory records repository evidence, not vendor-dashboard assumptions. `PASS` is used
only for checks executed against the input SHA or the current stage diff. External accounts,
credentials, production data, and payment-provider telemetry were not accessed.

## Decisions

| Tool or control | Decision | Repository evidence and current version | Access/data and CI cost | Verification | Rollback/removal |
| --- | --- | --- | --- | --- | --- |
| Graphify | KEEP | Project skill marker and local CLI are both `0.9.31`. `.graphifyignore` scopes dependencies/build output and `.gitignore` excludes all `graphify-out/`. No reinstall or repository-wide graph was run. | Local source navigation only; generated graph may expose source structure, so it remains untracked. No CI cost. | `graphify --version`; `git check-ignore -v graphify-out/graph.json`. Both PASS. | Remove only local generated output or restore the previously pinned CLI; no tracked output is expected. |
| Sentry | CONFIGURE | Existing integration retained. Frontend `@sentry/nextjs` and backend `@sentry/node` are upgraded from `8.55.2` to exact `10.54.0`; no second error tracker is added. | Runtime events leave the app only when DSNs are configured. Both runtimes set `sendDefaultPii: false`; frontend sanitizers and the existing backend sanitizer reduce exposure. The official client instrumentation is compiled into the browser bundle, while initialization and network egress remain DSN-gated. Source-map plugin/upload work needs `SENTRY_AUTH_TOKEN`, `SENTRY_ORG`, and `SENTRY_PROJECT`; no token is stored in Git. | Node 20 audit: backend `1 low/0 moderate/0 high/0 critical`; frontend `0 low/3 moderate/0 high/0 critical`. Typecheck, tests, builds, Docker, and real Sentry ingestion are recorded separately in stage evidence. | Revert the SDK/config files. Remove deployment DSNs/tokens through the provider settings and rebuild frontend; never commit credentials. |
| Prometheus / `prom-client` | KEEP | Existing backend metrics use `prom-client@15.1.3` and an authenticated `/metrics` boundary. Payment-related metrics are frozen and unchanged. | Metrics stay inside the configured scraper boundary; production scraper/dashboard access was not used. No new CI service. | Backend tests/build and an isolated authenticated scrape; real scraper/alerts remain NOT RUN until provider access exists. | Disable scraper/alerts first, then revert nonfinancial config only. Do not change frozen financial metrics. |
| PostHog | CONFIGURE | Existing `posthog-js` integration is opt-in, disables autocapture, and uses explicit events. No second analytics product is added. | Browser analytics leave the app only with `NEXT_PUBLIC_POSTHOG_KEY`. Consent, retention, and live payload filtering need provider verification. | Frontend tests/build locally; real consent and ingestion verification is deferred to the observability stage. | Remove the public key, rebuild/redeploy, then revert provider changes. |
| Pino | CONFIGURE | Existing backend structured logger is retained (`pino@9.14.0` resolved). Redaction and audit-output tests already exist; broader nonfinancial console cleanup belongs to stage 12. | Logs may contain operational metadata; secrets must remain redacted. No external service or extra CI job. | Backend audit-redaction tests, full backend tests, and build. | Revert scoped logger changes; never weaken redaction to make tests pass. |
| Playwright | KEEP | Existing E2E runner is pinned to `1.61.1`, uses isolated Compose, one Chromium worker, failure traces/video/screenshots, and no second browser framework. | Synthetic isolated test data only. Existing E2E job cost remains; no new stage-0 browser job. | `docker compose -f docker-compose.e2e.yml config --quiet`; `node e2e/scripts/run.mjs`. | Revert test/config changes; reports and results remain ignored. |
| npm audit policy | KEEP | Existing fail-closed policy covers backend, frontend, and E2E production dependencies. Exact GHSA exceptions require expiry and documentation. Stage 0 removes the last exception. | Registry metadata only; no package install in the scheduled job. Four short jobs weekly (policy tests plus three project audits). | Policy tests 10/10 PASS; all three project audits PASS after remediation. | Revert workflow/policy changes. Do not restore an expired exception without a new documented review. |
| Weekly dependency audit | ADD | `.github/workflows/dependency-audit.yml` runs every Monday and on manual dispatch with read-only permissions and SHA-pinned actions. | Reads repository and npm registry only; no runtime secrets. Expected cost is four lightweight jobs without dependency installation. | Validate workflow syntax, then verify the first scheduled/manual exact-SHA run in GitHub Actions. | Revert the workflow file; the regular push audit in `ci.yml` remains fail-closed. |
| Node/npm/ESLint toolchain | CONFIGURE | `.nvmrc` pins Node `20.20.2`; all three package manifests declare npm `10.8.2`. Backend and frontend now run real ESLint `9.39.5` separately from TypeScript typecheck, and development Compose uses the same pinned Node image with `npm ci`. `@testing-library/jest-dom` remains on Node-20-compatible `6.9.1`; ESLint 10 is outside the current Next 15 peer contract. | Local and CI dependency metadata only. No provider access; clean installs and lint add ordinary CI time. | Clean `npm ci`, backend lint/typecheck/build, frontend lint/typecheck/tests/build, and development Compose config PASS. Lint has zero errors with 21 backend and 63 frontend warnings. | Revert manifests, locks, flat configs, `.nvmrc`, CI separation, and development Compose changes together. |
| Renovate | ADD | `renovate.json` is validated with Renovate `44.46.7`: weekly window, Dependency Dashboard, one patch/minor group, separate dashboard-approved majors, `minimumReleaseAge: 14 days`, and no automerge. Financial files and payment-provider packages are excluded. Renovate is the selected single bot; Dependabot is intentionally not added. | GitHub App activation requires repository-admin approval. Weekly PR cost only; no runtime or financial write access is granted. | `renovate-config-validator renovate.json` PASS. App installation, Dependency Dashboard, repository dry run, and first PR are BLOCKED until owner activation. | Remove the config, uninstall the app, and close its open PRs separately. |
| Dependabot | REJECT | Would duplicate the selected Renovate role. | Duplicate PRs and alerts add noise and maintenance cost. | Confirm no `.github/dependabot.yml`. | Not applicable. |
| CodeQL | ADD | `.github/workflows/codeql.yml` adds pinned CodeQL `v4.37.9` JS/TS analysis for `main` push, pull request, weekly schedule, and manual dispatch with only `contents: read` and `security-events: write`. | Read-only source analysis; no runtime secrets. GitHub Actions minutes increase on push/PR/schedule. | `actionlint 1.7.7` PASS and the action SHA matches the official tag. Provider execution is NOT RUN; authenticated inspection of default setup is BLOCKED, so an owner must ensure only one setup is active. | Disable one setup before enabling the other; revert the workflow if advanced setup is removed. |
| GitHub ruleset | ADD | Public API reports `main.protected=false` and zero repository rulesets. The exact owner procedure for no force-push, no deletion, linear history, and retained direct push is in `docs/delivery-safety.md`. | Server-side admin change; no source or runtime data. | BLOCKED: export pre-change JSON, apply through authenticated GitHub settings/API, then read the effective ruleset back. | Restore the exported settings; Git revert cannot undo a server-side ruleset. |
| npm/GHA cache | KEEP | `actions/setup-node` is SHA-pinned and caches backend/frontend npm downloads by their lockfiles. | Cache contains public dependency artifacts, not runtime secrets. Existing CI cost reduction. | Inspect exact-SHA Actions logs for restore/save and record timings. | Remove cache inputs; caches are disposable. |
| Docker Buildx/GHA cache | ADD | CI pins Buildx setup `v4.3.0` and Bake `v7.3.0`, primes a named `type=gha` cache, then still runs the canonical fail-closed `docker compose --profile '*' build`. | Image layers may contain build inputs, so secrets must never be mounted into cached layers. Adds cache storage but should reduce build minutes. | Action SHAs match official tags; `actionlint` and Compose config PASS. Real cache import/export and unchanged image acceptance are NOT RUN until the stage-4 milestone push. | Revert Buildx/Bake/cache steps and delete only the `production-images` cache scope. |
| `verify:all` | ADD | `scripts/verify-all.mjs` mirrors every D5 policy, backend, frontend, Compose/image, E2E, and full-history Gitleaks gate. It pins Node/npm and Gitleaks checksums, uses disposable PostgreSQL 16/Redis 7, clears provider credentials, and reports status, exit code, and duration. | Local/CI orchestration only; no production/provider data or credentials are required. Full execution consumes the same build/test resources as D5. | Runner tests 5/5 PASS; `--list` and side-effect-free `--dry-run` PASS. Full D5 is deliberately NOT RUN until milestone 4 under the approved gating plan. | Revert the runner/tests and CI runner-test job while keeping the underlying fail-closed checks. |
| ast-grep and dependency-cruiser | DEFER (stage 11) | Not present. They are useful only with tested rules for server/client, direct fetch, module boundaries, and frozen finance imports. | Local source analysis; additional install and CI time. | Every rule needs positive and negative fixtures before enforcement. | Remove rules/packages and their dedicated CI command. |
| Knip | DEFER (stage 11) | Not present. Dynamic entries, migrations, and financial code make immediate deletion unsafe. | Local source analysis; report-only first. | Review every finding directly and require characterization tests before deletion. | Remove config/package; restore any separately proven entry point. |
| Context7 / Playwright MCP | DEFER | Not required by the reproducible CI runner and not used in stage 0. | External helper access must not receive secrets or production data. | Use only for a measured local diagnostic benefit; CI remains ordinary Playwright. | Disconnect/remove the helper without changing CI. |
| Serena, Semble, CodeRabbit, Sonar, second review bot | REJECT | Duplicate or unproven for the current repository. | Extra data exposure, permissions, notifications, and cost. | No installation/configuration should be present. | Not applicable. |

## External state and manual actions

| Item | Status | Evidence or required action |
| --- | --- | --- |
| GitHub Actions baseline | PASS | Exact stage-0 SHA `effe378949bd0f246abb96b60dad2e83ff86ed38` passed run `33084105336` with 10/10 jobs. Stage-1 workflow execution remains NOT RUN until the stage-4 milestone push. |
| Branch protection/ruleset | FAIL | Public GitHub API reports `main` unprotected and no rulesets. Add the limited direct-main ruleset in stage 1 only after exporting current settings. |
| Vercel/Railway promotion gate | BLOCKED | Commit statuses for the input SHA appeared within seconds of the push, while CI completed about eight minutes later. Public status data does not reveal whether they were preview, staging, or production. An authenticated owner must verify that production promotion waits for exact-SHA CI success. |
| Sentry ingestion/source maps/alerts | NOT RUN | No real DSN or auth token was used. Verify synthetic client and server errors, release association, scrubbed payloads, uploaded source maps, and alerts in a non-production Sentry project. |
| Prometheus scraper/dashboards/alerts | NOT RUN | Repository code exists, but external scraper and alert-manager settings were not accessible. |
| PostHog consent/ingestion/retention | NOT RUN | Repository code exists, but project settings and live payloads were not accessed. |
| Renovate GitHub App | BLOCKED | The local configuration is validated; app installation, Dependency Dashboard, and repository dry run need repository-admin action. |
| CodeQL default setup | BLOCKED | Authentication is required to determine whether default setup is enabled before adding advanced setup. |
| New CodeQL and Buildx jobs | NOT RUN | Local syntax, config, and action-pin checks PASS. Provider execution and a real GHA cache hit require the next milestone push. |

## Stage 0 rollback note

Revert the stage-0 Sentry dependency/configuration, audit allowlist, scheduled workflow, and
documentation as one logical change. Provider credentials and dashboards are external state
and must be removed through their owners; no credential was created or committed here.
